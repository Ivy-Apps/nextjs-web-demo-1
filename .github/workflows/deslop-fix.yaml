name: Deslop Fix

on:
  workflow_dispatch:

jobs:
  deslop-fix:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: ivy-apps/deslop-action@v1
        with:
          mode: fix
          gemini-api-key: ${{ secrets.GEMINI_API_KEY }}
