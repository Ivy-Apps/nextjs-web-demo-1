name: Deslop Check

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  deslop:
    runs-on: ubuntu-latest
    steps:
      - uses: ivy-apps/deslop-action@main
        with:
          mode: check
